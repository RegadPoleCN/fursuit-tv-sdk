
# Today 探索

返回适合在 Today 页面侧栏或详情页继续浏览的公开探索内容。

权限节点：`furtv.today.explore`

## 请求头

- `X-Api-Key: <apiKey>`

## 请求地址

- `GET /api/proxy/furtv/today/explore`

## 查询参数

- `exclude_today_id`：可选，排除某条 Today
- `limit`：可选，返回数量，上游最大值为 24

## 成功响应示例

```json

```
