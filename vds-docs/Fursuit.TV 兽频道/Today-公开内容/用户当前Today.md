
# 用户当前 Today

查询指定用户当天公开的 Today。

权限节点：`furtv.today.users.current`

## 请求头

- `X-Api-Key: <apiKey>`

## 请求地址

- `GET /api/proxy/furtv/today/users/:userIdentifier/current`

## 路径参数

- `userIdentifier`：用户 ID 或用户名

## 查询参数

- `explore_limit`：可选，附带探索内容数量

## 成功响应示例

```json

```
