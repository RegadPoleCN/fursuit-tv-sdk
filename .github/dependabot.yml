version: 2
updates:
  # Gradle dependencies (weekly check)
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Shanghai"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "gradle"
    commit-message:
      prefix: "chore(deps)"
    reviewers:
      - "RegadPoleCN"
    groups:
      kotlin-dependencies:
        patterns:
          - "kotlin*"
          - "org.jetbrains.kotlin:*"
          - "org.jetbrains.kotlinx:*"
        update-types:
          - "minor"
          - "patch"

      ktor-dependencies:
        patterns:
          - "io.ktor:*"
        update-types:
          - "minor"
          - "patch"

    ignore:
      # examples/java、examples/jvm 内引用的是本 SDK 自身（examples 文件夹不参与 Dependabot 更新）
      - dependency-name: "com.furrist.rp:fursuit-tv-sdk"

  # GitHub Actions (monthly check)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Shanghai"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(ci)"
    reviewers:
      - "RegadPoleCN"
